version: "3"

services:
  lims-frontend:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: lims-frontend
    ports:
      - "1002:3000"
    environment:
      - NODE_ENV=production
    volumes:
      - .:/app
    #command: npm run start
